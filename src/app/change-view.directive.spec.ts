import { ChangeViewDirective } from './change-view.directive';

describe('ChangeViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeViewDirective();
    expect(directive).toBeTruthy();
  });
});
